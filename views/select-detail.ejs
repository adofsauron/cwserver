<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "www.mobiletrain.org /TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="www.mobiletrain.org ">
<head>
    <meta charset="UTF-8">
	<meta name="viewport" content="width=device-width,initial-scale=1,user-scalable=no"/>
	<meta name="apple-mobile-web-app-capable" content="yes"/>
	<meta name="apple-mobile-web-app-status-bar-style" content="black"/>
	<meta name="format-detection" content="telephone=no,email=no"/>
	<title>食客</title>
	<link rel="stylesheet" type="text/css" href="/css/common.css">
	<link rel="stylesheet" type="text/css" href="/css/index.css">
	<script src="/js/jquery-1.9.1.min.js"></script>
	<script src="/js/common.js"></script>
	<script>
	//适配		
		$(window).on('resize', function () {
			var w = $(window).width();
	    	$('html').css('font-size',(w/375*100) + 'px') ;
	 	}).triggerHandler('resize') ;
	</script>

	
</head>

  <body>

    <% 
        function GetJLen (Json) {
            var len = 0
            for (var i in Json) {
                len += 1;
            }
            return len;
        }

        var dt = JSON.parse(ret);
    %>

    <header>
		<a class="back" href="javascript:history.back(-1)">
			<img src="/image/icon/back.png">
		</a>
        <p><%=dt[0].loc_cfy%></p>
	</header>
  
    <div class="wrapper">
    <%
        for (var i in dt) {
            var pic_main = JSON.parse(dt[i].pic_main);
    %>

        <a class="section" href="/shop-detail/<%=dt[i].id%>.action">
			<img src="/upload/<%=dt[i].uid%>/<%=pic_main[1]%>">
            <h1><%= dt[i].name %></h1>
			<p><span>48</span>元／位</p>
		</a>
    <%       
        }
    %>
    </div>

  </body>
</html>
